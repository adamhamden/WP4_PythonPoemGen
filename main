#!/usr/bin/env python

import sys
import requests


num_args = len(sys.argv)
if num_args > 2:
	print("Please only enter one keyword")
	exit()

keyword = sys.argv[1]


page = requests.get("http://dataquestio.github.io/web-scraping-pages/simple.html")

print(page.content)